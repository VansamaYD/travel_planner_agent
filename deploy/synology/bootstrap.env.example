# Keep this file mode 0600. It is the only secret required before first start.
APP_ENV=production
APP_MASTER_KEY=REPLACE_WITH_AT_LEAST_32_RANDOM_BYTES

# API keys are configured from the administrator settings page and encrypted
# into config/runtime/runtime-integrations.enc. They do not need to be added here.
